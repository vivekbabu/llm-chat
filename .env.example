# Copy this file to `.env` in the repo root (or export the var directly:
# export ANTHROPIC_API_KEY=<your-key>) before running the app.
ANTHROPIC_API_KEY=

# Optional: shows the real exception text in the chat instead of a generic
# "please refine your query" message. Leave unset in normal use.
DEBUG=
